## s3lim mcp

Run the MCP server over stdio

### Synopsis

Starts a Model Context Protocol (MCP) server communicating over stdio. Exposes s3lim optimization reports to LLMs.

```
s3lim mcp [flags]
```

### Options

```
  -h, --help               help for mcp
      --log-file string    Path to the offline s3lim report log file
      --log-group string   CloudWatch Log Group to query latest scan results from
```

### Options inherited from parent commands

```
      --custom-prefix strings   Explicit custom prefixes to track (can specify multiple times)
      --log-level string        Log level (debug, info, warn, error) (default "info")
      --profile string          AWS profile name to use
      --role-arn string         AWS IAM Role ARN to assume
```

### SEE ALSO

* [s3lim](s3lim.md)	 - s3lim is a high-performance S3 inventory analysis tool

###### Auto generated by spf13/cobra on 31-Jul-2026
